{
  "_comment": "Which template preset each stack token from kaizen-detect resolves to. Mirrors the 'Preset selection logic' table in docs/architecture.md. A stack the detector can emit but that is missing here fails the templates suite; one that resolves to `generic` warns, because detection is promising adaptation the templates do not deliver.",
  "stack_to_preset": {
    "generic": "generic",
    "typescript": "typescript-node",
    "javascript": "typescript-node",
    "frontend": "typescript-node",
    "backend-node": "typescript-node",
    "python": "python",
    "go": "generic",
    "rust": "generic",
    "java": "generic",
    "ruby": "generic",
    "php": "generic",
    "elixir": "generic"
  }
}
