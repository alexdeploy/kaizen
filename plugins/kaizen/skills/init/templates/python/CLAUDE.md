# Project conventions

> Project: **{{PROJECT_NAME}}** · Stack: {{STACK_FRIENDLY}} · PM: `{{PACKAGE_MANAGER}}`
> Generated by kaizen. Keep this under ~200 lines.

## Commands

- Install: `{{PACKAGE_MANAGER}} install`
- Test: `pytest`
- Lint: `ruff check`
- Format: `ruff format`
- Typecheck: `mypy .`

> If any command above doesn't exist in the project, kaizen will have removed it. See the drift report.

## Stack

- Language: Python 3.x (target the version pinned in `pyproject.toml` / `.python-version`)
- Package manager: {{PACKAGE_MANAGER}}
- Test runner: {{TEST_RUNNER}}
<!-- KAIZEN_ENRICH:framework_stack -->

## Conventions

- **Type hints everywhere.** Use modern syntax: `list[int]`, `dict[str, X]`, `X | None`.
- **Module-level functions over deep class hierarchies.** Prefer composition.
- **No `from x import *`.** Explicit imports only.
- **Docstrings for public functions** — one-line summary, blank line, body if needed.
- **Use `pathlib.Path`**, not `os.path`.
- **Errors are typed.** Custom exceptions per domain, not `raise Exception`.

## Architecture (brief)

<!-- KAIZEN_ENRICH:architecture_layout -->

For path-specific conventions, see [.claude/rules/](./.claude/rules/).

## Never do

- No direct commits to `main`.
- No `print()` for logging — use `logging` module.
- No bare `except:` — always catch specific exceptions.
- No mutable default arguments (`def f(x=[])`).
